# Trello Sprint Tracker Backend

Backend API for the Trello Sprint Tracker frontend.

## Features
- Fetches Trello board data
- Calculates totals, members, burndown points
- Endpoint: `/api/report/:boardId`

## Setup
```bash
npm install
npm run dev